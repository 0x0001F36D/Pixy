version: '1.0.0.{build}'
image: Visual Studio 2017
branches:
  only:
  - master
  
platform:
  - Any CPU

configuration: DEBUG

before_build:
  - nuget restore

for:
-
  matrix:
    only:
      - image: Visual Studio 2017